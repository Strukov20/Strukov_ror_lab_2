<%= link_to "upload new photo", new_photo_path, class: "btn btn-default" %>
<br/>

<% if @photos == [] %>
  Photos ninch
<% else %>
  <% @photos.each do |photo| %>
    <%= img_tag photo %>
    <%= photo.name %>
  <% end %>
<% end %>
